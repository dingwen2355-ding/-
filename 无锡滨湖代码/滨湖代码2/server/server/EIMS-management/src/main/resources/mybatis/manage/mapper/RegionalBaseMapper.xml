<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.znv.manage.dao.RegionalBaseDao">

    <resultMap id="map" type="java.util.HashMap">
        <result property="areaCode" column="area_code"/>
        <result property="areaName" column="area_name"/>
    </resultMap>

    <select id="getProvince"  resultMap="map" parameterType="String">
        SELECT * FROM t_dict_area_code where area_code like CONCAT('%','0000');
    </select>


    <select id="getCity"  resultMap="map" parameterType="String">
        SELECT * FROM t_dict_area_code where area_code LIKE CONCAT(#{province},'__','00') AND area_code!= CONCAT(#{province},'0000');
    </select>

    <select id="getCounty"  resultMap="map" parameterType="String">
        SELECT * FROM t_dict_area_code where area_code LIKE CONCAT(#{city},'%') ;
    </select>

    <select id="getRegionalName"  resultType="String" parameterType="String">
        SELECT area_name FROM t_dict_area_code where area_code=#{areaCode};
    </select>

</mapper>