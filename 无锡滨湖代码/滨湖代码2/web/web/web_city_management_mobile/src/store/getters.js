const getters = {
  documentVisibility: (state) => state.app.documentVisibility,
  windowInfoDetail: (state) => state.map.windowInfoDetail,
  mapClickGps: (state) => state.map.mapClickGps
}
export default getters
