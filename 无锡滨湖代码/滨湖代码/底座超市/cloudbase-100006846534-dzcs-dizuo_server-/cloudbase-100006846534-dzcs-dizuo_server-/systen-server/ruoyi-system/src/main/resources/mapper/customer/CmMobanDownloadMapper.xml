<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.customer.mapper.CmMobanDownloadMapper">


    <select id="top10Download" resultType="java.util.Map">
        SELECT
               A.ID,
               A.app_name,
               COUNT ( 1 ) counts
        FROM
            cm_app
                A JOIN cm_mobandownload M ON A.ID = M.app_id
        WHERE
            A.del_flag = '0'
        GROUP BY
            A.ID,
            A.app_name
        ORDER BY
            counts DESC
            LIMIT 10
    </select>
</mapper>
