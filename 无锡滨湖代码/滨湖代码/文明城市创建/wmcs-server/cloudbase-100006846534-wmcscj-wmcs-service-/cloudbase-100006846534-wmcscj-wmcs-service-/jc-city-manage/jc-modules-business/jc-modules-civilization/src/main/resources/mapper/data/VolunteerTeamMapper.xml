<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wxgis.jc.civilization.data.mapper.VolunteerTeamMapper">

    <select id="selectPageCustom" resultType="cn.wxgis.jc.civilization.data.resp.DataVolunteerTeamResponse">
        select * from (
            select data.* from data_volunteer_team data
        ) t
        <where>
            ${ew.sqlSegment}
        </where>
    </select>

    <select id="findResponseById" resultType="cn.wxgis.jc.civilization.data.resp.DataVolunteerTeamResponse">
        select data.* from data_volunteer_team data
            where data.id = #{id}
    </select>
</mapper>