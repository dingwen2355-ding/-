<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wxgis.jc.civilization.data.mapper.CheckItemMapper">

    <select id="selectPageCustom" resultType="cn.wxgis.jc.civilization.data.resp.DataCheckItemResponse">
        select * from (
            select data.*, jdata.name type_name, et.name event_type_name
            from data_check_item data
            left join data_check_points_type jdata on jdata.id = data.type_id
            left join quota_event_type et on et.id = data.event_type_id
        ) t
        <where>
            ${ew.sqlSegment}
        </where>
    </select>

</mapper>