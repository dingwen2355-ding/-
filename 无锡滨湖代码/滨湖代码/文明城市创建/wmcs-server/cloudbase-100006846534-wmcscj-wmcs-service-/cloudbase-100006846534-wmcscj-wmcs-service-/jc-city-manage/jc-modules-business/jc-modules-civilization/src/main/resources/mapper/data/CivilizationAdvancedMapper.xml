<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wxgis.jc.civilization.data.mapper.CivilizationAdvancedMapper">

    <select id="selectPageCustom"
            resultType="cn.wxgis.jc.civilization.data.resp.DataCivilizationAdvancedResponse">
        select * from (
            select data.*, data.region_code dept_id from data_civilization_advanced data
        ) t
        <where>
            ${ew.sqlSegment}
            ${params.dataScope}
        </where>
    </select>

    <select id="findResponseById"
            resultType="cn.wxgis.jc.civilization.data.resp.DataCivilizationAdvancedResponse">
        select data.*, jdata.name region_name
        from data_civilization_advanced data
                 left join sys_dept jdata on jdata.id = data.region_code
        where data.id = #{id}
    </select>
</mapper>