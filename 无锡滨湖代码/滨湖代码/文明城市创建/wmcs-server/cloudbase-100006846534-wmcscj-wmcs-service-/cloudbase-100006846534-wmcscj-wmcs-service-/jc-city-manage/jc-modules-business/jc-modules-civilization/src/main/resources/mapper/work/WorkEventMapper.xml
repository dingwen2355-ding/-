<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wxgis.jc.civilization.work.mapper.WorkEventMapper">

    <select id="selectPageCustom" resultType="cn.wxgis.jc.synch.wmb.resp.WorkEventResponse">
        select * from (
            select event.* from work_event event
        ) t
        <where>
            ${ew.sqlSegment}
        </where>
    </select>
</mapper>
