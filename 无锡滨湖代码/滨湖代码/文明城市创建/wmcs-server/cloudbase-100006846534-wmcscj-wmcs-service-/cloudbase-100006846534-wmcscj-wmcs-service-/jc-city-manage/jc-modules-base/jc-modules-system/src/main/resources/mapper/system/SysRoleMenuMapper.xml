<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wxgis.jc.system.web.mapper.SysRoleMenuMapper">

    <select id="listByRoleId" resultType="cn.wxgis.jc.system.resp.SysRoleMenuResponse">
        select rr.*, m.parent_id menuParentId
        from sys_role_menu rr
                 left join sys_menu m on m.id = rr.menu_id
        <where>
            and del_flag = 0
            and rr.role_id = #{roleId}
        </where>
        order by m.order_num asc
    </select>

</mapper>
