<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wxgis.jc.system.web.mapper.SysRegionMapper">

    <select id="voList" resultType="cn.wxgis.jc.system.resp.SysRegionResponse">
        select r.*, rr.region_name parentName
        from sys_region r
        left join sys_region rr on rr.id = r.parent_id
        <where>
            and r.del_flag = 0
            <if test="regionName != null and regionName != ''"> and r.region_name like concat('%', #{regionName},'%')</if>
        </where>
        order by r.order_num, r.region_code, r.create_time desc
    </select>
</mapper>
