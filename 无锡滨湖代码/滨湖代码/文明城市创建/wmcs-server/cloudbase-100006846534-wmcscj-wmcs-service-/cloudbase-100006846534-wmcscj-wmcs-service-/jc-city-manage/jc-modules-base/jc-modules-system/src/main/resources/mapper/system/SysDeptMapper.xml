<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wxgis.jc.system.web.mapper.SysDeptMapper">

    <select id="voList" resultType="cn.wxgis.jc.system.resp.SysDeptResponse">
        select t.* from (
            select d.id dept_id, d.*, pd.name parentName, (select count(1) from data_check_user cu where cu.dept_id = d.id) volunteer_count, ud.name wechat_dept_name
            from sys_dept d
            left join sys_dept pd on pd.id = d.parent_id
            left join unify_dept ud on ud.id = d.wechat_dept_id
            <where>
                and d.del_flag = 0
                <if test="name != null and name != ''"> and d.name like concat('%', #{name},'%')</if>
                <if test="type != null and type != ''"> and d.type = #{type} </if>

            </where>
        ) t
        <where>
            <!-- 数据范围过滤 -->
            ${params.dataScope}
        </where>
        order by type desc, order_num, create_time desc
    </select>

    <select id="findResponseById" resultType="cn.wxgis.jc.system.resp.SysDeptResponse">
        select d.*, pd.name parentName from sys_dept d left join sys_dept pd on pd.id = d.parent_id
        where d.id = #{id}
    </select>

</mapper>
