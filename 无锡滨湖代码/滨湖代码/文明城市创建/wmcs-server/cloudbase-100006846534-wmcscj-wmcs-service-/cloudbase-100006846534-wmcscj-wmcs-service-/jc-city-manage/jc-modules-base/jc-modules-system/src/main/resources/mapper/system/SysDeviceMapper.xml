<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wxgis.jc.system.web.mapper.SysDeviceMapper">

    <select id="selectPageCustom" resultType="cn.wxgis.jc.system.resp.SysDeviceResponse">
        select * from (
            select d.*, u.nick_name user_name, u.phonenumber
            from sys_device d
            left join sys_user u on u.id = d.user_id
        ) t
        <where>
            ${ew.sqlSegment}
        </where>
    </select>
</mapper>